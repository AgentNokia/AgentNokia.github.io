export class ubo {
    size: number;
    constructor () {
      this.size = 0;
    }

    /*
    npm install react
    npm install react-dom
    npm install @types/react
    npm install @types/react-dom

    inport React from 'react'
    inport { createRoor } from 'react-dom/client'

    jsx='react' // tsconfig
    npm install @rollup/plaugin-commonjs
    npm install @rollup/plugin-node-resolve
    npm install @rollup/plugin-replace
    
    
    */
    // create(gl: WebGL2RenderingContext, name: string) {
    //     let id = gl.getUniformBlockIndex(1, name);
    //     gl.bindBuffer(GL_UNIFORM_BUFFER, OE2_RndUBOs[OE2_RndUBOsSize].UBOId);
    //     gl.bufferData(GL_UNIFORM_BUFFER, NumOfBlocks * 16, NULL, GL_STATIC_DRAW);
    //   
    //     if (Bits != NULL)
    //       glBufferSubData(GL_UNIFORM_BUFFER, 0, NumOfBlocks * 16, Bits);
    //     gl.bindBuffer(GL_UNIFORM_BUFFER, 0);
    //   
    // }
}